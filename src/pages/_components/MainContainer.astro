---
import Container from "@/lib/components/Container.astro";

type Props = {
  class?: string;
};

const props = Astro.props;
---

<Container class={`sm:px-8 sm:max-w-xl lg:px-16 ${props.class}`}>
  <slot />
</Container>
